[package]
name = "lucid-tutor"
version = "0.1.0"
edition = "2021"
description = "The endless tutor engine for luciddreamer.ai — adapts, iterates, resonates"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
